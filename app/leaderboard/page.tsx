"use client";
import { useMemo } from "react";
import { useStreamStock } from "@/lib/useStreamStock";
import { money } from "@/lib/market";
const names=["ClipWhale","MoonBagz","PurpleBull","ChatAlpha","FomoLord","CandleGoblin","EmoteFund","SubPrime","HypeCycle","GreenCandle","ByteBroker","RiskyRizz","MarketMod","PrimePumper","DipBuyer"];
export default function Leaderboard(){const app=useStreamStock(); const users=useMemo(()=>[{name:app.state.username,cash:app.state.cash,portfolio:app.portfolioValue,change:app.totalReturn},...Array.from({length:100},(_,i)=>{const cash=2500+(100-i)*410+Math.sin(i)*900; const portfolio=5000+(100-i)*890+Math.cos(i)*1600; return {name:names[i%names.length]+(i+1),cash,portfolio,change:Math.sin(i/3)*1800}})].sort((a,b)=>(b.cash+b.portfolio)-(a.cash+a.portfolio)).slice(0,100),[app.state.username,app.state.cash,app.portfolioValue,app.totalReturn]);return <><section className="panel full"><p className="eyebrow">Leaderboard</p><h1>Top 100 richest users</h1><p className="muted">Production version will read this from Supabase profiles, cash balances, and holdings.</p></section><section className="panel full"><div className="table-wrap"><table><thead><tr><th>Rank</th><th>User</th><th>Cash</th><th>Portfolio</th><th>Net Worth</th><th>Daily Change</th></tr></thead><tbody>{users.map((u,i)=><tr key={u.name}><td>#{i+1}</td><td><strong>{u.name}</strong></td><td>{money(u.cash)}</td><td>{money(u.portfolio)}</td><td><strong>{money(u.cash+u.portfolio)}</strong></td><td className={u.change>=0?"gain":"loss"}>{u.change>=0?"+":""}{money(u.change)}</td></tr>)}</tbody></table></div></section></>}
